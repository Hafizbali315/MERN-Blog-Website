import { Helmet } from 'react-helmet';

const Home = () => {
	return (
		<>
			<Helmet>
				<title>Web articles</title>
				<meta name="description" content="learn HTML, CSS, JavaScript, Vue, ReactNative, React, Vue, Angular etc" />
			</Helmet>
		</>
	);
};

export default Home;
